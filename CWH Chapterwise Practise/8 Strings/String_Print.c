#include <stdio.h>

int main(){
    char arr[]= {'R', 'i', 's', 'h', 'i', 't', 'g', 'r', 'g' };
    printf("%s", arr);
    for (int i = 0; i < 9; i++)
    {
        printf("%c", arr[i]);
    }

    
    return 0;
}